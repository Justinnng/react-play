import React from 'react';
import PhotoContent from '../components/PhotoContent.jsx';

export default class Home extends React.Component {
  render() {
    return (
      <PhotoContent />
    );
  }
}